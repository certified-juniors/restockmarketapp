<h2 class = "vstack gap-2 col-md-5 mx-auto" style = "margin-top: 20px">Вход</h2>



{{#if isAuthorised}}
    <p>Вы вошли как {{user.login}}</p>
    <p>Ваш пароль: {{user.password}}</p>
{{else}}
    {{#if error}}
        <p>
            <h5 class = "vstack gap-2 col-md-5 mx-auto text-danger" style = "margin-top: 20px">{{error}}</h5>
        </p>
    {{/if}}

    <form action = "/login" method = "POST">
        <div class=" vstack gap-2 col-md-5 mx-auto">

            <div class="col-auto">
                <label for="Email" class="col-form-label">Email</label>
            </div>

            <div class="col-auto">
                <input type="text" name = "email" id="email" class="form-control" for = "Email" required>
            </div>

            <div class="col-auto">
                <label for="Password" class="col-form-label">Пароль</label>
            </div>

            <div class="col-auto">
                <input type="password" name = "password" id="password" class="form-control" for = "Password" required>
            </div>

            <button class = "btn" style = "background-color: dodgerblue; color: white; margin-top: 10px" type = "submit" style = "margin-top: 15px">Войти</button>
        </div>
        </div>
    </form>

{{/if}}